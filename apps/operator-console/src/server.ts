/**
 * Operator Console — 웹 UI + REST API (준비 중)
 */

import { createLogger } from '@sangfor/workflow-shared';

const log = createLogger('operator-console');

log.info('Operator Console is under development');
log.info('Planned features:');
log.info('  - Web UI for pipeline management');
log.info('  - Health check dashboard');
log.info('  - Wiki sync status');
log.info('  - REST API endpoints');

// TODO: Express/Fastify 서버 구현
// TODO: 파이프라인 관리 UI
// TODO: 실장비 점검 대시보드
// TODO: Obsidian 연동 UI
